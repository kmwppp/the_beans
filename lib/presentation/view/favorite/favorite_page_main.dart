import 'package:flutter/material.dart';

class FavoritePageMain extends StatelessWidget {
  const FavoritePageMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
